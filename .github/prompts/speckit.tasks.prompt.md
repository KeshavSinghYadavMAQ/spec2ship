---
agent: speckit.tasks
---

Run the `speckit.tasks` agent. Apply the shared [Agent Hook Policy](../agent-hook-policy.md)
and always generate executable harness tasks plus contract-test tasks for exposed contracts.
