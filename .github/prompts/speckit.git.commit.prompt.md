---
agent: speckit.git.commit
---

Run the `speckit.git.commit` agent from the repository root. Review the diff and obtain explicit
confirmation before creating a commit; never amend or rewrite history.
