---
agent: speckit.git.initialize
---

Run the `speckit.git.initialize` agent from the repository root. Preserve existing Git state and do
not create commits, branches, remotes, hooks, or credentials.
