---
agent: speckit.git.feature
---

Run the `speckit.git.feature` agent from the repository root. Preserve uncommitted work and require
confirmation before switching branches when the worktree is dirty.
